---
name: User Profile
description: Who the user is and how they prefer to work on this project
type: user
originSessionId: 85bf65ab-b6ea-4aed-9239-95c58dca3690
---
Vietnamese developer/designer working on a PTE practice platform (PTEUni). Communicates in Vietnamese.

## Preferences

- Requests broad, sweeping changes ("completely different interface") rather than incremental tweaks — prefers bold design decisions over conservative ones.
- Does not need step-by-step explanations of what was changed; trusts the implementation.
- Prefers to review changes visually in the browser rather than reading code diffs.
- Asks for structural setups (CLAUDE.md, .claude settings, memory) proactively — values organized project scaffolding.

## Communication Style

- Communicates in Vietnamese; respond in Vietnamese when the conversation is in Vietnamese.
- Prefers concise responses — no long explanations unless the user asks "why".
- Cancels tool calls that run servers or background processes without prior discussion; always ask before running `npm run dev` or background processes.
